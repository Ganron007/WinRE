# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# --- Local-only internal artifacts (never commit / never publish) ---
# Improvement plan, VM access, internal architecture, install wiring.
# Public release keeps docs/ free of lab IPs, hostnames and keys.
internal/
docs/internal/
docs/MALCAT.md

# --- Agent-tool personal config ---
.commandcode/

# --- Runtime-generated (source of truth lives in ops/smoke_flare.py) ---
ops/_smoke_pycompile.py

# --- Runtime state (never commit) ---
# Secrets / license keys (MALCAT_KEY etc.)
.env
.env.*
!*.env.template

# Detonation logs and artifact packs
logs/
local-runs/
cache/

# Frida / Procmon / FakeNet runtime output
*.pml
*.pcap
*.pcapng
*.jsonl

# --- Build output (x64dbg-MCP Zig plugin) ---
dist/
zig-out/
.zig-cache/

# --- IDA / Ghidra / BN databases ---
*.i64
*.idb
*.nam
*.bndb
*.gpr
*.rep

# --- Vendored third-party trees (users fetch these upstream; our changes
#     ship as .patch files in tools/) ---
integrations/

# --- OS / editor ---
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*~

# runtime-generated remote helper
winre/_remote_quick_helper.py
winre/_remote_dynamic_helper.py
winre/_remote_sql_helper.py
winre/__remote_*.py
